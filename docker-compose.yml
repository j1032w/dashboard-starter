version: '3.4'

services:
  dashboard-starter-ui:
    container_name: dashboard-starter-ui
    image: dashboard-starter-ui:${TAG:-develop}

    build:
      context: .
      dockerfile: Dockerfile


    ports:
      - 4200:80

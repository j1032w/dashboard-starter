import { Args, Query, Resolver } from '@nestjs/graphql';import {ListingService} from '../core/applications/real-estate';import {ListingQuery} from '../infrastructures/shared';import { RealEstateListSchema } from './real-estate-list.schema';@Resolver()export class RealEstateListResolver {  constructor(    private readonly residentialPropertyService: ListingService,  ) {}  @Query(() => [RealEstateListSchema])  async residentialProperties(    @Args('filter', { type: () => ListingQuery })    filter: ListingQuery,  ) {    return await this.residentialPropertyService.findAsync(filter);  }}
import { Injectable } from '@nestjs/common';import { ConfigService } from '@nestjs/config';@Injectable()export class HomeService {  constructor(private readonly configService: ConfigService) {}  appInfo() {    return { name: this.configService.get('appConfig').name };  }  getMongoDbConnection() {    const mongoConfig = this.configService.get('mongodbConfig');    return `MongoDB URI: ${mongoConfig.mongoUri}/${mongoConfig.mongoDbName}`;  }}